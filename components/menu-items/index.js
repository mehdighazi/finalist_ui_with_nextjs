
import pages from './pages';
import other from './other';

// ==============================|| MENU ITEMS ||============================== //

export const menuItems = {
    items: [[], pages, [], []]
};

export const BreadcrumbsItems={
    items: [other]
}
